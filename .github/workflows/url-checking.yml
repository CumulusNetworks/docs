name: 'URL Checker'

on:
  push:
    branches:
      - plumbis-stage

jobs:
  check_urls:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: plumbis/hugo-linkcheck-action@v0.1.4