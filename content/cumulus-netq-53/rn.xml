<tables>
<table name="Open Issues in 5.2.1">
<tr>
<th> Issue ID </th>
<th> Description </th>
<th> Affects </th>
<th> Fixed </th>
</tr>
<tr>
<td>4977619</td>
<td>NetQ might fail to load in high-availability (HA) scale deployments when ECMP is enabled. To work around this issue, disable ECMP ingestion by updating the Kafka connector configuration:
&lt;ol&gt;
&lt;li&gt;Create a backup of the current configuration:
&lt;code&gt;kubectl get kafkaconnector cassandra-sink-group-1 -n netq-infra -o yaml &amp;gt; /tmp/cassandra-sink-group-1.backup.yaml&lt;/code&gt;
&lt;/li&gt;
&lt;li&gt;Capture the current Kafka connector values:

OLD_KCQL=$(kubectl get kafkaconnector cassandra-sink-group-1 -n netq-infra -o jsonpath='{.spec.config.connect.cassandra.kcql}')
OLD_TOPICS=$(kubectl get kafkaconnector cassandra-sink-group-1 -n netq-infra -o jsonpath='{.spec.config.topics}')

&lt;/li&gt;
&lt;li&gt;Remove ECMP-related entries from KCQL and topics:

NEW_KCQL=$(echo "$OLD_KCQL" | tr ';' '\n' | grep -v -E 'INTO ecmp_info SELECT|INTO ecmp_info_aggregate SELECT|INTO ecmp_info_group_aggregate SELECT' | paste -sd ';' -)
NEW_TOPICS=$(echo "$OLD_TOPICS" | tr ',' '\n' | grep -v -E '^netq_obj_ecmp_info$|^netq_obj_ecmp_info_aggregate$|^netq_obj_ecmp_info_group_aggregate$' | paste -sd ',' -)

&lt;/li&gt;
&lt;li&gt;Verify the changes before applying them:

echo "=== KCQL ECMP entries remaining (should only show ecmp_hash + arEcmpInfo) ==="
echo "$NEW_KCQL" | tr ';' '\n' | grep -i ecmp
echo "=== TOPICS ECMP entries remaining (should only show netq_obj_ecmp_hash) ==="
echo "$NEW_TOPICS" | tr ',' '\n' | grep -i ecmp
echo "Old KCQL: $(echo "$OLD_KCQL" | tr ';' '\n' | wc -l) / New: $(echo "$NEW_KCQL" | tr ';' '\n' | wc -l) (-3)"
echo "Old TOPICS: $(echo "$OLD_TOPICS" | tr ',' '\n' | wc -l) / New: $(echo "$NEW_TOPICS" | tr ',' '\n' | wc -l) (-3)"

&lt;/li&gt;
&lt;li&gt;Apply the updated configuration:
&lt;code&gt;kubectl patch kafkaconnector cassandra-sink-group-1 -n netq-infra --type=merge -p "$(jq -n --arg k "$NEW_KCQL" --arg t "$NEW_TOPICS" '{spec:{config:{"connect.cassandra.kcql":$k,"topics":$t&amp;#125;&amp;#125;&amp;#125;')"&lt;/code&gt;
&lt;/li&gt;
&lt;/ol&gt;
To revert the changes, restore the backup with {{kubectl apply -f /tmp/cassandra-sink-group-1.backup.yaml}}.
</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4977342, 4889408</td>
<td>The NetQ UI might not display a complete list of interfaces. To work around this issue, restart the NetQ agent or upgrade to the latest NetQ version.</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4964170</td>
<td>When you create a partition, the operation might fail with a BAD_PARAM error. To work around this issue, retry the operation until it is successful. </td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4897897</td>
<td>The NetQ NVLink API might fail to return a 400 error response when unsupported parameters are included in the API request.</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4894672, 4896363</td>
<td>NVOS upgrades on switches might fail if the image filename contains spaces or special characters.</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4890084</td>
<td>The NetQ CLI might not delete threshold-crossing alerts configured for ACL resources. To work around this issue, use the UI to delete the alerts.</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4876932</td>
<td>The NMX controller service might intermittently fail to switch to an alternate out-of-band (OOB) port when the primary registration port becomes unavailable. As a result, partition management operations might not function correctly on the secondary OOB port if the registered management port goes down.</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4867933</td>
<td>Threshold-crossing events created before version 5.1.0 may not display event values correctly after you upgrade NetQ.</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4839716, 4844441</td>
<td>You cannot perform lifecycle management operations immediately after backing up an HA scale cluster deployment. To work around this issue, wait several hours before initiating LCM operations.</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4838526</td>
<td>NMX controller and telemetry services might display a DOWN status if the primary out-of-band (OOB) management port is unavailable.</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4830357</td>
<td>When you try to filter images in the NetQ UI based on image type, NetQ might ignore the filter.</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4794266</td>
<td>Power sensor (PSU) events might show inconsistent sensor names. Additionally, NetQ might not generate PSU events reliably.</td>
<td>5.1.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4784336</td>
<td>The NetQ for NVLink deployment option is not supported in air-gapped environments.</td>
<td>5.0.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4780773</td>
<td>The NetQ for NVLink Swagger UI might incorrectly display 500 status placeholder text in example responses.</td>
<td>5.0.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4687477</td>
<td>When you run a validation against a group of devices with specific labels, NetQ ignores any pre-configured filters.</td>
<td>5.0.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4681581</td>
<td>The {{netq bootstrap reset purge-db}} command might take up to 60 minutes to complete on Base Command Manager scale deployments.</td>
<td>5.0.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4399074</td>
<td>When connecting a switch to NMX-T or NMX-C through the service registration workflow, use either the IP address or the hostname. Using both creates duplicate registrations, and the operation does not fail as expected.</td>
<td>5.0.0-5.2.1</td>
<td></td>
</tr>
<tr>
<td>4100882, 4119697</td>
<td>When you attempt to export a file that is larger than 200MB, your browser might crash or otherwise prevent you from exporting the file. To work around this issue, use filters in the UI to decrease the size of the dataset that you intend to export.</td>
<td>4.12.0-4.15.1, 5.0.0-5.2.1</td>
<td></td>
</tr>
</table>
<table name="Fixed Issues in 5.2.1">
<tr>
<th> Issue ID </th>
<th> Description </th>
<th> Affects </th>
</tr>
</table>
<table name="Open Issues in 5.2.0">
<tr>
<th> Issue ID </th>
<th> Description </th>
<th> Affects </th>
<th> Fixed </th>
</tr>
<tr>
<td>4965251</td>
<td>When BlueField DPUs are in the default, embedded mode, the output of the {{netq check roce}} command might produce duplicate entries for the same device (one for the host and one for the DPU). This issue can also affect the calculations for the RoCE Mode Consistency and DSCP Classification tests. Additionally, the {{netq show roce-config host}} command might display DPU interfaces alongside host interfaces. This issue does not affect DPUs that are configured in separated mode.</td>
<td>5.2.0</td>
<td></td>
</tr>
<tr>
<td>4867933</td>
<td>Threshold-crossing events created before version 5.1.0 may not display event values correctly after you upgrade NetQ.</td>
<td>5.1.0-5.2.0</td>
<td></td>
</tr>
<tr>
<td>4784336</td>
<td>The NetQ for NVLink deployment option is not supported in air-gapped environments.</td>
<td>5.0.0-5.2.0</td>
<td></td>
</tr>
<tr>
<td>4687477</td>
<td>When you run a validation against a group of devices with specific labels, NetQ ignores any pre-configured filters.</td>
<td>5.0.0-5.2.0</td>
<td></td>
</tr>
<tr>
<td>4681581</td>
<td>The {{netq bootstrap reset purge-db}} command might take up to 60 minutes to complete on Base Command Manager scale deployments.</td>
<td>5.0.0-5.2.0</td>
<td></td>
</tr>
<tr>
<td>4399074</td>
<td>When connecting a switch to NMX-T or NMX-C through the service registration workflow, use either the IP address or the hostname. Using both creates duplicate registrations, and the operation does not fail as expected.</td>
<td>5.0.0-5.2.0</td>
<td></td>
</tr>
<tr>
<td>4100882, 4119697</td>
<td>When you attempt to export a file that is larger than 200MB, your browser might crash or otherwise prevent you from exporting the file. To work around this issue, use filters in the UI to decrease the size of the dataset that you intend to export.</td>
<td>4.12.0-4.15.1, 5.0.0-5.2.0</td>
<td></td>
</tr>
</table>
<table name="Fixed Issues in 5.2.0">
<tr>
<th> Issue ID </th>
<th> Description </th>
<th> Affects </th>
</tr>
<tr>
<td>4977619</td>
<td>NetQ might fail to load in high-availability (HA) scale deployments when ECMP is enabled. To work around this issue, disable ECMP ingestion by updating the Kafka connector configuration:
&lt;ol&gt;
&lt;li&gt;Create a backup of the current configuration:
&lt;code&gt;kubectl get kafkaconnector cassandra-sink-group-1 -n netq-infra -o yaml &amp;gt; /tmp/cassandra-sink-group-1.backup.yaml&lt;/code&gt;
&lt;/li&gt;
&lt;li&gt;Capture the current Kafka connector values:

OLD_KCQL=$(kubectl get kafkaconnector cassandra-sink-group-1 -n netq-infra -o jsonpath='{.spec.config.connect.cassandra.kcql}')
OLD_TOPICS=$(kubectl get kafkaconnector cassandra-sink-group-1 -n netq-infra -o jsonpath='{.spec.config.topics}')

&lt;/li&gt;
&lt;li&gt;Remove ECMP-related entries from KCQL and topics:

NEW_KCQL=$(echo "$OLD_KCQL" | tr ';' '\n' | grep -v -E 'INTO ecmp_info SELECT|INTO ecmp_info_aggregate SELECT|INTO ecmp_info_group_aggregate SELECT' | paste -sd ';' -)
NEW_TOPICS=$(echo "$OLD_TOPICS" | tr ',' '\n' | grep -v -E '^netq_obj_ecmp_info$|^netq_obj_ecmp_info_aggregate$|^netq_obj_ecmp_info_group_aggregate$' | paste -sd ',' -)

&lt;/li&gt;
&lt;li&gt;Verify the changes before applying them:

echo "=== KCQL ECMP entries remaining (should only show ecmp_hash + arEcmpInfo) ==="
echo "$NEW_KCQL" | tr ';' '\n' | grep -i ecmp
echo "=== TOPICS ECMP entries remaining (should only show netq_obj_ecmp_hash) ==="
echo "$NEW_TOPICS" | tr ',' '\n' | grep -i ecmp
echo "Old KCQL: $(echo "$OLD_KCQL" | tr ';' '\n' | wc -l) / New: $(echo "$NEW_KCQL" | tr ';' '\n' | wc -l) (-3)"
echo "Old TOPICS: $(echo "$OLD_TOPICS" | tr ',' '\n' | wc -l) / New: $(echo "$NEW_TOPICS" | tr ',' '\n' | wc -l) (-3)"

&lt;/li&gt;
&lt;li&gt;Apply the updated configuration:
&lt;code&gt;kubectl patch kafkaconnector cassandra-sink-group-1 -n netq-infra --type=merge -p "$(jq -n --arg k "$NEW_KCQL" --arg t "$NEW_TOPICS" '{spec:{config:{"connect.cassandra.kcql":$k,"topics":$t&amp;#125;&amp;#125;&amp;#125;')"&lt;/code&gt;
&lt;/li&gt;
&lt;/ol&gt;
To revert the changes, restore the backup with {{kubectl apply -f /tmp/cassandra-sink-group-1.backup.yaml}}.
</td>
<td>5.1.0</td>
</tr>
<tr>
<td>4977342, 4889408</td>
<td>The NetQ UI might not display a complete list of interfaces. To work around this issue, restart the NetQ agent or upgrade to the latest NetQ version.</td>
<td>5.1.0</td>
</tr>
<tr>
<td>4964170</td>
<td>When you create a partition, the operation might fail with a BAD_PARAM error. To work around this issue, retry the operation until it is successful. </td>
<td>5.1.0</td>
</tr>
<tr>
<td>4897897</td>
<td>The NetQ NVLink API might fail to return a 400 error response when unsupported parameters are included in the API request.</td>
<td>5.1.0</td>
</tr>
<tr>
<td>4894672, 4896363</td>
<td>NVOS upgrades on switches might fail if the image filename contains spaces or special characters.</td>
<td>5.1.0</td>
</tr>
<tr>
<td>4890084</td>
<td>The NetQ CLI might not delete threshold-crossing alerts configured for ACL resources. To work around this issue, use the UI to delete the alerts.</td>
<td>5.1.0</td>
</tr>
<tr>
<td>4876932</td>
<td>The NMX controller service might intermittently fail to switch to an alternate out-of-band (OOB) port when the primary registration port becomes unavailable. As a result, partition management operations might not function correctly on the secondary OOB port if the registered management port goes down.</td>
<td>5.1.0</td>
</tr>
<tr>
<td>4839716, 4844441</td>
<td>You cannot perform lifecycle management operations immediately after backing up an HA scale cluster deployment. To work around this issue, wait several hours before initiating LCM operations.</td>
<td>5.1.0</td>
</tr>
<tr>
<td>4838526</td>
<td>NMX controller and telemetry services might display a DOWN status if the primary out-of-band (OOB) management port is unavailable.</td>
<td>5.1.0</td>
</tr>
<tr>
<td>4830357</td>
<td>When you try to filter images in the NetQ UI based on image type, NetQ might ignore the filter.</td>
<td>5.1.0</td>
</tr>
<tr>
<td>4794266</td>
<td>Power sensor (PSU) events might show inconsistent sensor names. Additionally, NetQ might not generate PSU events reliably.</td>
<td>5.1.0</td>
</tr>
<tr>
<td>4780773</td>
<td>The NetQ for NVLink Swagger UI might incorrectly display 500 status placeholder text in example responses.</td>
<td>5.0.0-5.1.0</td>
</tr>
</table>
</tables>