# Error: NVIDIA.CodeCase
#
# Checks for use of capitalization in content tagged as commands.
#
extends: capitalization
message: '"%s" inside a command should be lowercase.'
level: error
scope: code
match: $lower
exceptions:
  # Don't check MD file header content
  - '[\-]{3}.[\-]{3}'